export default function RestaurantsPage() {
  return (
    <main style={{ padding: '24px', fontFamily: 'Arial, sans-serif' }}>
      <h1>Restaurants</h1>
      <p>Restaurants page is being set up.</p>
      <p>Use /api/sync to import data from Google Sheets.</p>
    </main>
  )
}